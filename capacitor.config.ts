import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Buscador de repositorios',
  webDir: 'dist'
};

export default config;
